name: tirth_today
description: "my portfolio site"
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  url_launcher:
  font_awesome_flutter:
  google_fonts:
  video_player:
  page_curl_effect:
  just_audio:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
  assets:
    - 'assets/'